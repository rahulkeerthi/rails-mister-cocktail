<%= simple_form_for(cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :category, collection: %w[fizz highball sour stirred] %>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
<% end %>
